from item import Item
# from phone import Phone
# Now can access these classes from separate file

item1 = Item("MyItem", 750)
item1.name = "OtherItem"        # This allows you to change the given value

# Encapsulation is when you don't allow an attribute to be changed after it is first initialised
