# When to use class or static

# Use a static method when the function isn't unique per instance
# E.g. just checking if number is an integer

# Use class method to instantiate from a data structure

# Main difference is that static isn't passing object parameter
# It has no required parameters